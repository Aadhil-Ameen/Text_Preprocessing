# Text_Preprocessing
Web Analytics Assignment for text preprocessing and finding its similarity .
The dataset file is too long to display so download the raw file and check the dataset for it.
Use Google Collab to run the code and attach the dataset file as "Twitter_Data" and it will automatically get downnloaded as .csv file and it will print the output 
